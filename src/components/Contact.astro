---
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
---

<section
  i<section
  id="contact"
  class="w-full py-12 md:py-24 lg:py-32 bg-gray-100 dark:bg-gray-800"
>
  <div class="container mx-auto px-4 md:px-6">
    <div class="flex flex-col items-center space-y-4 text-center">
      <div class="space-y-2">
        <h2 class="text-3xl font-bold tracking-tighter sm:text-4xl md:text-5xl">
          ¿Listo para Empezar?
        </h2>
        <p
          class="mx-auto max-w-[600px] text-gray-500 md:text-xl/relaxed lg:text-base/relaxed xl:text-xl/relaxed dark:text-gray-400"
        >
          Contáctanos hoy para modernizar tu menú y mejorar la experiencia de
          tus clientes.
        </p>
      </div>
      <div class="w-full max-w-sm space-y-2">
        <form class="flex flex-col space-y-4">
          <Input client:load type="text" placeholder="Nombre" />
          <Input client:load type="email" placeholder="Correo electrónico" />
          <Textarea client:load placeholder="Mensaje" />
          <Button client:load type="submit">Enviar Mensaje</Button>
        </form>
      </div>
    </div>
  </div>
</section>
